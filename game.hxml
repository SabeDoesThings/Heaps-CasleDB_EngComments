# Connecting the heaps library
-lib heaps
# Connecting the hldx library (runs on Windows only)
# Alternatively, you can use hlsdl
-lib hldx
# We'll need a castle library to work with CastleDB
-lib castle
# We borrow the primary class of the application with the entry point
-main Game
# Output file
-hl game.hl
